# 待整合修改记录

**处理任何语料任务时，读完 `SKILL.md` 和领域 SKILL 后，必须再读本文件。**
这里的条目是**已经生效但还没写进 SKILL 的规则**，优先级等同于它标注的层级。

## 怎么用

想到一条小改动时：

1. 在下面「待整合」里加一条；
2. 如果它需要 GPT 立刻照做，同步补进 ChatGPT 项目指令（或让 Claude Code 改
   `GPT项目说明.md` 后你重新粘一次）；
3. **不要**为此重构 `SKILL.md`。

攒到一定数量、或某次本来就要大改 SKILL 时，让 Claude Code 一次性整合：把条目写进
`SKILL.md` 或对应领域 SKILL 的正确位置，然后把条目挪到下面的「已整合」并注明整合到哪里。

这样 SKILL 保持稳定，小改动不会让两个模型的规则版本来回漂移。

---

## 待整合

### 2026-08-11 · 问"怎么设计/怎么运行"时先给机制，不给设计哲学　【领域 · `语料/AI/SKILL.md` §7】

来源：AI/05《Codex 系统设计》第一版读后反馈（2026-08-10 第 1 轮，已记入该文反馈区）。
用户原话："我要看的是技术不是设计哲学""后面 deepseekv4pro 会对 codex 做特殊优化，
所以我需要知道原理才能和 AI 更好的协作"——**读这类文章的目的是拿去和模型协作改系统，
讲不出数据结构和执行路径就等于没写。**

在 `语料/AI/SKILL.md` §7 现有第 3 条之后插入一条（其余五条 GPT 的建议与现有 §7 逐字相同，
不用动）：

```markdown
- 当用户询问系统“怎么设计、怎么运行”时，必须先解释具体的数据结构、消息顺序、接口、状态变化、执行路径和失败恢复，再讨论设计取舍；不得用设计哲学替代技术机制；
```

**这条现在只有 Claude Code 侧生效。** `待整合.md` 不在 `上传给GPT/` 的六个文件里，GPT 看不到；
它在 v2 那次会话里是自己想出来的，换个新会话就忘了。所以这条最终必须落进 `语料/AI/SKILL.md`，
落完重跑 `python3 tools/export_for_gpt.py` 并重传六个文件——在那之前，凡是让 GPT 写
AI 系统类文章，都要在提问里手动带上这条。

### 2026-08-11 · 两处检查清单里的「无外部资源」作废　【全局 · `SKILL.md` §6、§10】

`SKILL.md` §6 第 1 条已放宽为"外部资源不禁止，只有公式不得只靠 CDN 渲染"，但同一份文件里
两处检查清单还留着旧口径，跟它直接打架：

- §6「交付前做结构性检查」：`没有任何外部资源引用；`
- §10「交付检查」：`HTML 自包含、无外部资源、无残留 LaTeX 定界符，公式为 MathML；`

**以 §6 第 1 条为准。** 自查时这两条按下面执行：外部样式表、字体、外链图片、文末参考文献
链接都不算违规，只查"公式是否只靠外部 KaTeX/MathJax 渲染而没有 MathML 兜底"。

`tools/intake.py` 已经是新口径（外部资源只警告不阻断，唯一阻断是 CDN 公式库 + 无 MathML），
所以收稿行为不受影响；受影响的只是照着清单自查的模型——它会以为自己交付不合格。

→ 下次整合时：§6 那条改成「公式不依赖外部脚本渲染（有 MathML 兜底）」，§10 那条改成
「公式为 MathML，无残留 LaTeX 定界符」，两处都去掉"自包含 / 无外部资源"的字样。
改完必须重跑 `python3 tools/export_for_gpt.py` 并重新上传六个文件。

---

## 已整合

### 2026-07-29 · 必须提供可下载的文件

正式交付必须是可下载的 `.html` 文件；只贴正文、只放 canvas、让用户自己复制粘贴都不算。
用户的阅读方式是把文件下载到平板打开，收稿脚本也只认下载目录里的真实文件。

→ 已整合进 `SKILL.md` §6 第 1 条；`GPT项目说明.md` 的「交付方式」还写明了**具体动作**
（用代码工具写文件并给下载链接），因为只说"要给文件"时 GPT 会把内容放进 canvas。

### 2026-07-29 · 放宽外部引用限制

原规则"不引用任何外部资源"改为：外部样式表、字体、外链图片**不禁止**（离线时降级但正文可读），
文末参考文献网址完全允许。

**唯一保留的硬限制**：公式不得只靠外部 KaTeX/MathJax 渲染且没有 MathML 兜底——
那不是降级而是彻底失效，地铁里公式一个都看不见，而显示公式正是本项目用 HTML 的唯一理由。

→ 已整合进 `SKILL.md` §6 第 1 条。`tools/intake.py` 同步调整：所有外部资源检查降为警告，
只保留"CDN 公式库 + 无 MathML"作为阻断。五种场景实测通过，正文提到 "KaTeX" 一词不会误伤。
