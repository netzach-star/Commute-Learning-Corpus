# 待整合修改记录

**处理任何语料任务时，读完 `SKILL.md` 和领域 SKILL 后，必须再读本文件。**
这里的条目是**已经生效但还没写进 SKILL 的规则**，优先级等同于它标注的层级。

## 怎么用

想到一条小改动时：

1. 在下面「待整合」里加一条；
2. 如果它需要 GPT 立刻照做，同步补进 ChatGPT 项目指令（或让 Claude Code 改
   `GPT项目说明.md` 后你重新粘一次）；
3. **不要**为此重构 `SKILL.md`。

攒到一定数量、或某次本来就要大改 SKILL 时，让 Claude Code 一次性整合：把条目写进
`SKILL.md` 或对应领域 SKILL 的正确位置，然后把条目挪到下面的「已整合」并注明整合到哪里。

这样 SKILL 保持稳定，小改动不会让两个模型的规则版本来回漂移。

---

## 待整合

### 2026-07-29 · 全局 · 必须提供可下载的文件

正式交付必须是**可下载的 `.html` 文件**。不接受只在聊天里贴正文、只放进 canvas、
或让用户自己复制粘贴保存。

**为什么**：用户的阅读方式是把文件下载到平板用浏览器打开；收稿脚本也只认下载目录里的
真实文件。没有文件就等于没交付。

**目前状态**：已写进 `GPT项目说明.md` 的「交付方式」。
**整合去向**：`SKILL.md` §6 HTML 交付。

### 2026-07-29 · 全局 · 允许带参考文献网址

文章可以在文末列出参考资料的网址，用普通链接即可：

```html
<li><a href="https://arxiv.org/abs/…">论文标题（arXiv）</a></li>
```

**这与「自包含、不引用外部资源」不冲突**：被禁止的是渲染时必须联网抓取的资源
（`<link>` 样式表、`<script src>`、`<img src>`、`@import`、CSS `url()`），
它们缺网就会让页面变形或功能失效。`<a href>` 只是一段文本，离线时点不开而已，
正文完全不受影响。已用 `tools/intake.py` 的校验器实测确认不会被拦。

参考资料**仍然没有硬性要求**：不规定最少条数、不要求记访问日期、经典结论不强制附链接
（见 `SKILL.md` §5.2）。这一条只是明确"想给链接可以给"。

**目前状态**：无需改动，现有规则和校验器都已兼容。
**整合去向**：`SKILL.md` §5.2 核验，作为一句澄清。

---

## 已整合

_（暂无）_
