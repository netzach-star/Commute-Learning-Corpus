---
name: commute-learning-corpus
description: 按用户当前要求生成中文 HTML 学习语料，追加读后反馈，更新领域学习画像与个性化写作要求，并维护 README.md 中的语料目录表。不用于自动选题、生产队列、新闻监控或调度 Agent。
---

# 通勤学习语料：全局规则

## 1. 项目边界

本项目只做三件事：

1. 按用户当前要求生产可直接阅读的学习语料；
2. 把用户读后反馈追加到原文，并把可复用经验写入正确层级的 Skill；
3. 维护 `README.md` 中的语料目录表。

不要在本项目中设计或维护候选主题、生产队列、新闻采集器或调度 Agent。正式成果必须写成仓库里的文件，不能只停留在聊天回复里。

用户按自己当时的想法下达任务，可能一次要求生成多篇。本项目没有定时节奏，也不需要替用户规划下一篇写什么。

## 2. 两层 Skill 与指令优先级

处理任何语料任务时，先读取本文件，再读取目标领域 Skill：

- `语料/数学/SKILL.md`
- `语料/计算机/SKILL.md`
- `语料/AI/SKILL.md`
- `语料/马列/SKILL.md`
- `语料/其他/SKILL.md`

本文件只保存跨领域且长期稳定的总体规则：项目边界与优先级、目录分类与编号、通用生产流程、HTML 交付要求、反馈闭环、README 目录表维护。

以下内容不得写入本文件，应写入领域 Skill 或原文章反馈区：

- 用户在某领域已经学到哪里、会什么或暂时不会什么；
- 当前教材、课程范围、学习主线和阶段目标；
- 只适用于单一领域的写法；
- 只适用于某篇文章或某个系列的临时要求。

发生冲突时按以下顺序处理：

1. **用户当前消息中的明确要求**；
2. **目标领域 Skill 中的最新学习画像**——"会不会、学到哪、忘了什么、当前教材是哪本"这类关于用户状态的事实，一律以领域 Skill 为准；
3. **当前文章或系列已经记录的约束**——只约束结构、风格和该系列的写法，不能用来推翻第 2 条的画像事实。例如某系列早期写着"每次都要重讲测度论基础"，而数学 Skill 现在标注该内容"已掌握"，以数学 Skill 为准；
4. **本文件的全局规则**。

本仓库不存在 `归档/` 目录，也不应该建立。如果在任何位置发现历史归档、早期 Agent 规划或废弃草稿，一律不作为规则来源，并向用户报告，不要试图调和它与现行 Skill 的矛盾。

## 3. 领域、目录与编号

### 3.1 五个领域

正式语料只写入这五个目录之一：

| 目录 | 收什么 |
| --- | --- |
| `语料/数学/` | 数学课程、教材章节、定理与证明、数学史 |
| `语料/计算机/` | 传统软件开发与软件工程、系统、网络、操作系统、产品与工程流程 |
| `语料/AI/` | 模型原理、LLM、Agent、Skill、记忆系统、人机协作 |
| `语料/马列/` | 马克思主义、列宁主义、毛泽东思想的**理论部分**及其思想史、实践争论、现实分析框架 |
| `语料/其他/` | 以上四类之外的一切 |

边界判据：

- **计算机 / AI**：移除 AI 后主要结论仍成立，归"计算机"；移除模型、Agent、Skill、AI 使用或人机协作后主题失去核心对象，归"AI"。AI 只作案例或辅助工具时不改变归属。
- **马列 / 其他**：只有以马克思主义理论本身为对象、或明确以马克思主义为分析框架的文章进"马列"。一般的经济学、历史、政治学主题，即使涉及左翼议题，只要不是在讲马克思主义理论，都进"其他"。
- **其他**：经济金融、历史、心理学、语言学、社会学、自然科学、新闻背景、临时学习主题等，全部进"其他"，不要为此新建领域目录。某一主题在"其他"里稳定成系列且规则足够独特时，由用户决定是否升格为独立领域。

`语料/其他资料（非html）/` 不是语料领域目录。它存放用户提供的原始素材（压缩包、笔记等），不参与编号，不在其中生成文章。

### 3.2 文件名与编号

```text
NN_主题系列_文章短标题.html
```

`NN` 为两位数字，每个领域从 `01` 起独立编号。

**编号来源是 `README.md` 的目录表**，取目标领域表中最大编号加一。因此：

- 生成前必须先同步仓库最新状态（`git pull`，或重新读取 README 原文），不能使用旧快照；
- 如果当前环境能直接列出目标目录，以目录里实际存在的文件为准，并顺手修正 README 与实际不符的地方；
- 一次生成多篇时，在同一批次内依次递增，批次内不得重号；
- 全部文件写完后更新一次 README 目录表，然后提交推送；
- 不得占用已存在的编号或文件名；不得依据聊天中说的"第几篇"编号。

发现文件名或系列顺序冲突时，先报告，不自行批量重排。

## 4. 修改权限

已交付的文章默认只读。

- **允许**：把用户的读后反馈原样追加到文末反馈区。**追加反馈不算修改文章**，不需要额外确认。
- **允许**：用户明确指出这篇写得失败、要求重写，或明确要求改动某处时，原地修改同一个文件，保留原编号和原文件名。
- **禁止**：没有上述明确指示时改动正文。
- **禁止**：用新编号或 `08b_` 之类的变体重新发布同一篇文章。
- **禁止**：删除或重写反馈区里的历史记录。反馈区只追加。

## 5. 通用生产流程

### 5.1 开始前

至少确定：

- 本篇真正要回答的核心问题；
- 所属领域和系列；
- 用户指定的阅读目标、材料和特殊要求；
- 与既有文章的依赖关系。

只有当歧义会实质改变主题、难度或结构时才提问；小的实现选择自行决定。

### 5.2 核验

- 当前事实、版本、历史细节、现实数据和高风险信息必须核验；
- 成熟主题优先查找权威材料、一手资料和长期被引用的原始文章；
- 区分来源事实、作者观点、学界解释和本文推断；
- **不伪造书目、页码、引文或链接**；
- 有可直接访问的原始地址时给出，不用聚合页或低质量转载替代原文。

参考资料目前**没有**硬性要求：不规定最少条数，不要求记访问日期，数学等经典结论不强制附链接。"如何确保来源真实且信息齐全"是项目二阶段的工作，当前不要为此增加流程。

### 5.3 写作

默认目标为一次约 30—35 分钟的通勤阅读，通常约 12,000—16,000 个中文字符。主题自然需要更短或更长时，以讲清核心问题为准，不重复凑字数。这个口径不必因"难文章读不完"而下调——用户的实际通勤时间有富余，读不完的部分会在后续时间补上。

所有领域共同遵守：

- 围绕一个清楚的核心问题组织全文；
- 不用术语、人物、软件或工具名称清单代替解释；
- 说明对象为何出现、各部分怎样连接、解决了什么以及边界在哪里；
- 新概念首次出现时就地解释，不要求读者频繁回翻；
- 至少使用一个完整例子承载主线；是否使用反例、证明或系统流程由领域 Skill 决定；
- 保持足够的知识密度和论证完整性。降低的是未解释术语、跨段依赖和无谓上下文切换，不是有效信息量；
- 结尾压缩本篇核心结论，并说明它在更大知识结构中的位置。

**术语中英对照**：专业术语首次出现时给出中英对照，写作 `中文（English）`，例如"测度（measure）""幂等（idempotence）""注意力（attention）"。四个领域都适用，**数学也不例外**。判据是这个词是否需要用英文去检索原始文献；已经完全通用的词（函数、集合、代码）不必标注。同一术语只在首次出现时标注一次。

## 6. HTML 交付

用 HTML 只有一个理由：**让数学公式在平板浏览器里直接渲染出来**。用户把文件下载到平板，用浏览器打开阅读。

除此之外**对版式和外观没有要求**。不要在样式上额外投入，不要为"统一规范"引入共享模板，也不要回头重排已有文章的样式或元数据。

硬性要求只有四条：

1. **单个完整、自包含的 `.html` 文件**，不引用任何外部资源——不用 CDN、外部字体、外部脚本或外链图片。用户的平板打开文件时可能没有网络。
2. 基本头部：

   ```html
   <!doctype html>
   <html lang="zh-CN">
   <head>
     <meta charset="utf-8">
     <meta name="viewport" content="width=device-width, initial-scale=1">
     <title>...</title>
   </head>
   ```

3. **数学公式使用可直接显示的 MathML**，并在 `<annotation encoding="application/x-tex">` 中保留原始 TeX。不要输出 `$...$`、`$$...$$`、`\(...\)`、`\[...\]` 这类需要外部脚本解析的写法——没有 MathJax/KaTeX 会渲染它们。
4. 文末保留反馈区，即使当前为空：

   ```html
   <section id="reader-feedback">...</section>
   ```

交付前做结构性检查（不需要真的开浏览器渲染）：

- 文档结构完整、标签闭合、内部锚点可用；
- 正文里没有残留未转换的 LaTeX 定界符；
- 没有任何外部资源引用；
- 反馈区存在，且旧反馈完整未丢失。

`<head>` 里的语料编号、领域、日期等元数据是**可选的**。现有文章的写法并不一致，不必统一。

聊天里只简洁报告完成状态和文件路径，不要把整篇 HTML 正文贴出来。

## 7. README 目录表

`README.md` 里的 `<!-- INDEX:START -->` / `<!-- INDEX:END -->` 之间是语料目录表，它是**编号的唯一权威来源**，供无法列出目录的 Agent 续号。

每次新增文章后，必须在同一轮：

1. 把新文章按领域加入表格（编号、文件名、标题）；
2. 更新表头的"最后更新"日期；
3. 与代码一起提交推送。

只有新增或重命名文件才需要改这里；追加反馈不改目录表。

仓库里的 `tools/build_index.py` 可以扫描 `语料/` 重新生成这段目录表——在能执行命令的环境里优先用它，避免手写出错。

收下外部（如 ChatGPT）生成的文章时，用 `tools/intake.py` 代替手工提交：它会先校验新增和改动的
HTML（自包含性、残留 LaTeX、反馈区、编号占用、重新生成时的截断），再更新目录表并提交推送。
校验不通过会中止且不提交。

## 8. 动态学习画像与领域规则

领域 Skill 是会变化的当前状态，不是一次写定的背景简介。

当用户明确说明以下任一变化时，必须在同一轮更新相应领域 Skill：

- 已学、已掌握、正在学习或已经遗忘某项知识；
- 对某个概念仍不熟悉，或先前判断有误；
- 当前教材、课程范围、学习主线或阶段目标变化；
- 对该领域语料的结构、难度、严格性、案例或解释方式提出可复用要求。

维护时遵守：

- 使用 `已掌握 / 正在学习 / 不应默认 / 当前目标` 等清楚状态；
- 更新"最后整理日期"，替换过时结论，不在不同位置保留互相冲突的旧快照；
- 用户当前陈述优先于旧 Skill；
- 仅仅生成、保存或读过一篇文章，不自动等于已经掌握其中知识；
- 不从一个领域的能力推断另一个领域的水平；
- 跨领域都成立的要求才写回本文件。

## 9. 反馈闭环

用户读完文章后会把反馈追加在文章末尾，或直接在对话里说。收到反馈后：

1. 把用户原意追加到原 HTML 的 `reader-feedback`，不得覆盖旧记录；
2. 记录日期、轮次、问题分类、采取的处理和状态；
3. 只有反馈明确指出这篇写得失败或要求改动时，才按 §4 修订正文；否则不动正文；
4. 判断反馈属于单篇、领域还是全局；
5. 把可复用规则写入正确层级。

作用域判定：

- **单篇或系列**：事实纠错、局部结构调整，或只适用于该系列的要求，留在原文反馈区；
- **领域**：同类领域文章会反复受益，写入对应领域 Skill；
- **全局**：无论领域如何都成立，写入本文件。

推荐记录结构：

```html
<article class="feedback-entry" data-date="YYYY-MM-DD" data-round="N">
  <h3>YYYY-MM-DD · 第 N 轮</h3>
  <p><strong>原始反馈：</strong>……</p>
  <p><strong>问题分类：</strong>……</p>
  <p><strong>处理：</strong>……</p>
  <p><strong>Skill 更新：</strong>全局 / 领域 / 无，并说明规则</p>
  <p><strong>状态：</strong>待处理 / 部分处理 / 已处理</p>
</article>
```

不要因一次反馈删除既有有效规则。新旧要求冲突时，明确各自的适用条件。

## 10. 交付检查

- [ ] 已读取本文件和目标领域 Skill；
- [ ] 已同步仓库最新状态，编号取自 README 目录表当前最大值；
- [ ] 已使用领域 Skill 中最新的学习画像，而非历史快照；
- [ ] 文件进入正确领域目录，按本领域独立续号，未占用已有编号；
- [ ] 核心问题、阅读目标和资料边界已处理；
- [ ] 专业术语首次出现有中英对照；
- [ ] 没有术语堆叠、未解释概念或无依据降低知识密度；
- [ ] 事实无伪造，引用可核验；
- [ ] HTML 自包含、无外部资源、无残留 LaTeX 定界符，公式为 MathML；
- [ ] 文末反馈区存在且历史记录未丢失；
- [ ] 未在无明确指示的情况下改动已有文章正文；
- [ ] README 目录表已更新；
- [ ] 本次学习进度或个性化要求已更新到正确领域 Skill；
- [ ] 本次读后反馈已按单篇、领域或全局正确分流。
