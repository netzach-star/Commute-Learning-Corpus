---
name: commute-learning-corpus
description: 按用户当前要求生成、校验和迭代中文 HTML 学习语料，并维护两层生产规则。用于新建或修订学习文章、追加读后反馈、更新领域学习画像与个性化写作要求、维护语料目录；不用于自动选题、生产队列、新闻监控或调度 Agent。
---

# 通勤学习语料：全局规则

## 1. 项目边界

本项目只做两件事：

1. 按用户当前要求生产可直接阅读的学习语料；
2. 接收反馈，修订原文，并把可复用经验写入正确层级的 Skill。

不要在本项目中设计或维护候选主题、生产队列、新闻采集器或调度 Agent。正式成果必须保存到项目中，不能只停留在聊天回复。

## 2. 两层 Skill

处理任何语料任务时，先读取本文件，再读取目标领域 Skill：

- `语料/数学/SKILL.md`
- `语料/计算机/SKILL.md`
- `语料/AI/SKILL.md`
- `语料/马列/SKILL.md`
- `语料/其他/SKILL.md`

本文件只保存跨领域且长期稳定的总体规则，包括：

- 项目边界与指令优先级；
- 目录、分类和编号；
- 通用生产与核验流程；
- HTML 交付和反馈记录；
- 学习画像与个性化规则的维护方法。

以下内容不得写入本文件：

- 用户在某领域已经学到哪里、会什么或暂时不会什么；
- 当前教材、课程范围、学习主线和阶段目标；
- 只适用于数学、计算机、AI、马列或其他单一领域的写法；
- 只适用于某篇文章或某个系列的临时要求。

这些内容分别写入领域 Skill 或原文章反馈区。

发生冲突时按以下顺序处理：

1. 用户当前消息中的明确要求；
2. 当前文章或系列已经记录的约束；
3. 目标领域 Skill 中的最新学习画像和规则；
4. 本文件的全局规则；
5. `归档/` 不参与当前决策。

## 3. 领域、目录与编号

正式语料只写入一个目标目录：

- `语料/数学/`
- `语料/计算机/`
- `语料/AI/`
- `语料/马列/`
- `语料/其他/`

文件名使用：

```text
NN_主题系列_文章短标题.html
```

每个领域独立编号。生成前只扫描目标目录，以该目录现有 HTML 的最大编号续号；不得依据聊天中的“第几篇”或其他领域的编号。不得覆盖旧文章。

计算机与 AI 的边界按核心知识判断：

- 移除 AI 后主要结论仍成立，归入“计算机”；
- 移除模型、Agent、Skill、AI 使用或人机协作后主题失去核心对象，归入“AI”；
- AI 只是案例或辅助工具，不改变传统软件工程主题的归属。

发现文件名、HTML 元数据或系列顺序冲突时，先报告，不自行批量重排。

## 4. 通用生产流程

### 4.1 开始前

至少确定：

- 本篇真正要回答的核心问题；
- 所属领域和系列；
- 用户指定的阅读目标、材料和特殊要求；
- 与既有文章的依赖关系；
- 是否存在会实质改变讲解路线的歧义。

只有重大歧义会改变主题、难度或结构时才提问；小的实现选择自行决定。

### 4.2 核验

- 当前事实、版本、历史细节、现实数据和高风险信息必须核验；
- 成熟主题优先查找权威材料、一手资料和长期被引用的原始文章；
- 区分来源事实、作者观点、学界解释和本文推断；
- 不伪造书目、页码、引文或链接；
- 文末给出能够直接访问的原始地址，不用聚合页或低质量转载替代原文。

### 4.3 写作

默认目标为一次约 30—35 分钟的通勤阅读。通常为约 12,000—16,000 个中文字符；主题自然需要更短或更长时，以讲清核心问题为准，不重复凑字数。

所有领域共同遵守：

- 围绕一个清楚的核心问题组织全文；
- 不用术语、人物、软件或工具名称清单代替解释；
- 说明对象为何出现、各部分怎样连接、解决了什么以及边界在哪里；
- 新概念首次出现时就地解释，不要求读者频繁回翻；
- 概念密集或结构型文章在正文前给出只含 3—7 个核心节点的关系图，并说明连线含义；
- 至少使用一个完整例子承载主线；是否使用反例、证明或系统流程由领域 Skill 决定；
- 保持足够的知识密度和论证完整性。降低的是未解释术语、跨段依赖和无谓上下文切换，不是有效信息量；
- 结尾压缩本篇核心结论，并说明它在更大知识结构中的位置。

## 5. 正式 HTML

正式交付物必须是完整、自包含的 HTML，而不是 Markdown 或只存在于聊天中的正文。至少包含：

```html
<!doctype html>
<html lang="zh-CN">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>...</title>
</head>
<body>
  ...
  <section id="reader-feedback">...</section>
</body>
</html>
```

在 `<head>` 中保留语料编号、领域、生成或修订日期、预计阅读时间和当前状态等机器可读元数据。

数学公式在成品中使用可直接显示的 MathML，并在 `<annotation encoding="application/x-tex">` 中保留原始 TeX。不要依赖聊天界面解析 `$...$`、`$$...$$`、`\(...\)` 或 `\[...\]`。

交付前实际渲染并检查：

- 标题层级、目录锚点和链接；
- 行内公式、独立公式、矩阵和长公式；
- 是否残留未处理的 LaTeX；
- 桌面端和移动端是否溢出；
- 文末反馈区是否存在，旧反馈是否完整。

聊天中只简洁报告完成状态和项目路径，不嵌入整篇 HTML 正文。

## 6. 动态学习画像与领域规则

领域 Skill 是会变化的当前状态，不是一次写定的背景简介。

当用户明确说明以下任一变化时，必须在同一轮更新相应领域 Skill：

- 已学、已掌握、正在学习或已经遗忘某项知识；
- 对某个概念仍不熟悉或先前判断有误；
- 当前教材、课程范围、学习主线或阶段目标变化；
- 对该领域语料的结构、难度、严格性、案例或解释方式提出可复用要求。

维护时遵守：

- 使用 `已掌握 / 正在学习 / 不应默认 / 当前目标` 等清楚状态；
- 更新“最后整理日期”，替换过时结论，不在不同位置保留互相冲突的旧快照；
- 用户当前陈述优先于旧 Skill；
- 仅仅生成、保存或读过一篇文章，不自动等于已经掌握其中知识；
- 不从一个领域的能力推断另一个领域的水平；
- 跨领域都成立的要求才写回本文件。

## 7. 反馈闭环

收到读后反馈后：

1. 把用户原意追加到原 HTML 的 `reader-feedback`，不得覆盖旧记录；
2. 记录日期、轮次、问题分类、采取的修改和状态；
3. 修订文章并复查相关段落；
4. 判断反馈属于单篇、领域还是全局；
5. 把可复用规则写入正确层级。

作用域判定：

- **单篇或系列**：事实纠错、局部结构调整或只适用于该系列的要求，留在原文反馈区；
- **领域**：同类领域文章会反复受益，写入对应领域 Skill；
- **全局**：无论领域如何都成立，写入本文件。

推荐记录结构：

```html
<article class="feedback-entry" data-date="YYYY-MM-DD" data-round="N">
  <h3>YYYY-MM-DD · 第 N 轮</h3>
  <p><strong>原始反馈：</strong>……</p>
  <p><strong>问题分类：</strong>……</p>
  <p><strong>处理：</strong>……</p>
  <p><strong>Skill 更新：</strong>全局 / 领域 / 无，并说明规则</p>
  <p><strong>状态：</strong>待处理 / 部分处理 / 已处理</p>
</article>
```

不要因一次反馈删除既有有效规则。新旧要求冲突时，明确适用条件。

## 8. 交付检查

- [ ] 已读取根 Skill 和目标领域 Skill；
- [ ] 已使用领域 Skill 中最新的学习画像，而非历史快照；
- [ ] 文件进入正确目录并按本领域独立续号；
- [ ] 核心问题、阅读目标、资料边界和重大歧义已处理；
- [ ] 没有术语堆叠、未解释概念或无依据降低知识密度；
- [ ] 事实和参考资料可核验；
- [ ] HTML、MathML、链接和移动端布局已实际检查；
- [ ] 文末反馈区存在且历史记录未丢失；
- [ ] 本次学习进度或个性化要求已更新到正确领域 Skill；
- [ ] 本次读后反馈已按单篇、领域或全局正确分流。
